<?xml version="1.0" encoding="utf-8"?>
<!--
     Cortex disables allowBackup entirely (see AndroidManifest.xml). This file makes
     the "no data leaves the device via platform backup" stance explicit for anyone
     reading the manifest, per Security.md's "Unwanted cloud sync" threat priority.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapability="true">
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
